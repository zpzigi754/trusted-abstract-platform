module inner {
    var y: integer;

}

module main {
    var x: integer;
    instance inn: inner();
    next {
        if (x' == 0 && inn.y' == 0) {

        }
    }
    control {
        v = induction;
        check;
        print_results;
        
    }
}
